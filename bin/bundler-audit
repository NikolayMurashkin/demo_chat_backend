#!/usr/bin/env ruby
# frozen_string_literal: true

require "rubygems"
require "bundler/setup"

ARGV.concat(%w[--config .bundler-audit.yml]) if ARGV.empty? || ARGV.include?("check")

load Gem.bin_path("bundler-audit", "bundle-audit")
